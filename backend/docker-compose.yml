version: '3.8'

services:
  backend:
    build: .
    container_name: futsal-backend
    env_file:
      - .env
    ports:
      - "${PORT:-3000}:3000"
    # depends_on:
    #   - db
    networks:
      - futsal

networks:
  futsal:
    driver: bridge